<?xml version="1.0" encoding="utf-8"?>
<merge xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <me.aap.utils.ui.menu.OverlayMenuItemView
        android:id="@+id/tool_view_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:submenu="@layout/view_title_menu"
        app:text="@string/title" />

    <me.aap.utils.ui.menu.OverlayMenuItemView
        android:id="@+id/tool_view_subtitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:submenu="@layout/view_subtitle_menu"
        app:text="@string/subtitle" />

</merge>